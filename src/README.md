Components directory will house straight components, not reliant on Gatsby.
Gatsby stuff can live where it needs to.

Storybook will not render Gatsby views currently so we will use Gatsby for Gatsby.